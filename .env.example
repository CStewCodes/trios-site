# Neon Postgres connection string (server-only — never expose to the browser)
# Project: trios-site-waitlist
DATABASE_URL=

# Resend API key (server-only) — waitlist confirmation email after successful signup
RESEND_API_KEY=

# Optional From address. Default: TriOS <onboarding@resend.dev>
# RESEND_FROM=TriOS <onboarding@resend.dev>

